{
  "name": "nym-device-cleanup",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright scraper to remove registered devices from Nym account",
  "scripts": {
    "cleanup": "npx playwright test cleanup-devices.ts",
    "install-browsers": "npx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
